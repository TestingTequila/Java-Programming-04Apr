Arrays: In order to  hold multiple values[of same data type], we should prefer Arrays

dataType variableName=value;
int x =10;


dataType[] arrayName={value1, value2, value3.......valueN};

Issue with Arrays: this can hold only fixed values or you should use array only when you know the count of values stored will
                   always be fixed